# Local backend url: http://localhost:3001/api/v1
# Cloud backend url: https://workforcebd.onrender.com/api/v1
API_BASE_URL=http://localhost:3001/api/v1

# Log verbosity. One of: debug | info | warn | error.
# Server (route handlers, proxy). Defaults: debug in dev, info in prod.
LOG_LEVEL=debug

# Browser log verbosity (must be NEXT_PUBLIC_ to reach the client bundle).
NEXT_PUBLIC_LOG_LEVEL=debug

# Socket.IO origin for live notifications (path /socket.io). Same host as the
# backend, without the /api/v1 suffix. Must be NEXT_PUBLIC_ to reach the browser.
# NEXT_PUBLIC_SOCKET_URL=https://workforcebd.onrender.com
NEXT_PUBLIC_SOCKET_URL=http://localhost:3001

# Signs the admin console's inactivity stamp (HMAC-SHA256). Server-only — never
# expose it. Must be at least 32 chars; generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Without it the app falls back to a random per-process key, which forces admins
# to sign in again after every restart and breaks sessions across instances.
ADMIN_SESSION_SECRET=hithereisasecret
